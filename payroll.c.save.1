#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main(int argc, char *argv[]){
	/* do stuff */

	if (argc < 4) {
		puts("Please re-run with 4 arguments.");
		return 0;
	} else {
		char *lname = argv[1];
		char *fname = argv[2];
	double h	ours = atof(argv[3]);
	double rate = atof(argv[4]);
	printf("last %s\nfirst %s\n", lname, fname);
}
